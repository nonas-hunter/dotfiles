---
- hosts: localhost
  connection: local
  vars_files:
    - vars/main.yml
  tasks:
    - name: Display inventory hostname
      debug:
        msg: "Inventory hostname: {{ inventory_hostname }}"
    - name: Display system hostname
      debug:
        msg: "System hostname: {{ ansible_facts['hostname'] }}"
    - name: Print the FQDN
      ansible.builtin.debug:
        msg: "FQDN: {{ ansible_facts['nodename'] }}"
  roles:
    - package_manager
    - terminal_emulator
    - zellij
    - nodejs
    - uv
    - neovim
    - direnv

- hosts: work
  connection: local
  vars_files:
    - vars/main.yml
  tasks:
    - name: Display inventory hostname
      debug:
        msg: "Inventory hostname: {{ inventory_hostname }}"
    - name: Display system hostname
      debug:
        msg: "System hostname: {{ ansible_facts['hostname'] }}"
  roles:
    - cs

